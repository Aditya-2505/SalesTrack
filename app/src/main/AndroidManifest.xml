<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Hardware Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Required for scheduling alarms in Android 13+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SalesTrack">

        <!-- Launcher Activity: Login Screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Salesperson Home Screen -->
        <activity
            android:name=".SalespersonHomeActivity"
            android:exported="false" />

        <!-- Camera Screen -->
        <activity
            android:name=".CameraActivity"
            android:exported="false" />

        <!-- Trip History Screen -->
        <activity
            android:name=".TripHistoryActivity"
            android:exported="false" />

        <!-- Manager Dashboard Screen -->
        <activity
            android:name=".ManagerDashboardActivity"
            android:exported="false" />

        <!-- Salesperson Detail Screen -->
        <activity
            android:name=".SalespersonDetailActivity"
            android:exported="false" />

        <!-- Reports Screen -->
        <activity
            android:name=".ReportsActivity"
            android:exported="false" />

        <!-- Broadcast Receiver for Local Daily Alarms -->
        <receiver
            android:name=".notifications.AlarmReceiver"
            android:exported="false" />

    </application>

</manifest>
